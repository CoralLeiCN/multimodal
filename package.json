{
  "name": "multimodal",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "bun run --cwd frontend dev",
    "build": "bun run --cwd frontend build",
    "lint": "bun run --cwd frontend lint",
    "test": "bun run --cwd frontend test",
    "generate-client": "bun run --cwd frontend generate-client",
    "neon": "neon"
  },
  "devDependencies": {
    "@neon/config": "^1.7.2",
    "@neon/env": "^1.4.3",
    "neon": "^5.0.0"
  }
}
